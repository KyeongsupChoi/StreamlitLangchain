## Copy to .env and fill in values
#
# This project supports Groq via LangChain.
GROQ_API_KEY=

# Optional (defaults are set in code)
GROQ_MODEL=llama-3.1-8b-instant
GROQ_TEMPERATURE=0.2

# Standard model parameters (optional)
# If unset, LangChain/provider defaults are used where applicable.
GROQ_MAX_TOKENS=
GROQ_TIMEOUT_SECONDS=
GROQ_MAX_RETRIES=2

# Rate limiting (optional)
# Set GROQ_RATE_LIMIT_RPS to enable (requests/second). Examples:
# - 0.1  => ~1 request per 10 seconds
# - 1.0  => ~1 request per second
GROQ_RATE_LIMIT_RPS=
GROQ_RATE_LIMIT_BUCKET=10
GROQ_RATE_LIMIT_CHECK_SECONDS=0.1
